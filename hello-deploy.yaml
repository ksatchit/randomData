---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  annotations:
    flux.weave.works/automated: "true"
  name: hello-deploy
spec:
  replicas: 2
  template:
    metadata:
      labels:
        name: hello
    spec:
      containers:
        - name: flux-test
          image: openebs/flux-test:master-a000001
          ports:
            - containerPort: 8080
 
