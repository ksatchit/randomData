---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  annotations:
    flux.weave.works/automated: "true"
  name: hello-deploy
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: hello
    spec:
      containers:
        - name: flux-test
          image: openebs/flux-test:latest
          ports:
            - containerPort: 8080
 
